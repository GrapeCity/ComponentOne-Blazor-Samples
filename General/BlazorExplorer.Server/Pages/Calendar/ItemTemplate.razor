@page "/Calendar/ItemTemplate"
@using C1.Blazor.Calendar
@using C1.Blazor.Core
@using System;
@using Localization

<div >

    <ul class="status-list">
        <li>
            <span class="oi oi-warning" style="color: orange;">&nbsp; @CalendarRes.Incident</span>
        </li>
        <li>
            <span class="oi oi-bug" style="color: crimson;">&nbsp; @CalendarRes.IssueFound</span>
        </li>
        <li>
            <span class="oi oi-wrench" style="color: blue;">&nbsp; @CalendarRes.Maintenance</span>
        </li>
        <li>
            <span class="oi oi-check" style="color: green;">&nbsp; @CalendarRes.Good</span>
        </li>
    </ul>

    <div style="height: 550px; width: 550px">

        <C1Calendar Style="@_calendarStyle" HeaderStyle="@_calendarHeaderStyle" DayOfWeekStyle="@_calendarDayOfWeekStyle" DayStyle="@CalendarDayStyle" TodayStyle="@_calendarToDayStyle" SelectionStyle="@_calendarSelectionStyle">

            <DayOfWeekSlotTemplate>

               @switch (context.DayOfWeek)
                {
                    case DayOfWeek.Monday:
                        <span class="oi">@CalendarRes.Monday</span>
                        break;
                    case DayOfWeek.Tuesday:
                        <span class="oi">@CalendarRes.Tuesday</span>
                        break;
                    case DayOfWeek.Wednesday:
                        <span class="oi">@CalendarRes.Wednesday</span>
                        break;
                    case DayOfWeek.Thursday:
                        <span class="oi">@CalendarRes.Thursday</span>
                        break;
                    case DayOfWeek.Friday:
                        <span class="oi">@CalendarRes.Friday</span>
                        break;
                    case DayOfWeek.Saturday:
                        <span class="oi oi-flag" style="color: orange;">&nbsp;@CalendarRes.Saturday</span>
                        break;
                    case DayOfWeek.Sunday:
                        <span class="oi oi-flag" style="color: orange;">&nbsp;@CalendarRes.Sunday</span>
                        break;
                }

            </DayOfWeekSlotTemplate>


            <DaySlotTemplate>

                @if (((DateTime) context.Date).Day % 3 == 0)
                {
                    <span class="oi oi-warning" style="color: orange;">&nbsp;</span>
                    @context.Date.ToString("dd")
                }
                else if (((DateTime) context.Date).Day % 4 == 0)
                {
                    <span class="oi oi-bug" style="color: crimson;">&nbsp;</span>
                    @context.Date.ToString("dd")
                }

                else if (((DateTime) context.Date).Day % 7 == 0)
                {
                    <span class="oi oi-wrench" style="color: blue;">&nbsp;</span>
                    @context.Date.ToString("dd")
                }

                else
                {
                    <span class="oi oi-check" style="color: green;">&nbsp;</span>
                    @context.Date.ToString("dd")
                }



            </DaySlotTemplate>

        </C1Calendar>

    </div>




</div>

<br />
<br />
<br />


@code {
    readonly C1Style _calendarStyle = new C1Style()
    {
        Width = "100%",
        Height = "100%",

        BorderWidth = 1,
        BorderStyle = C1StyleBorderStyle.Solid,
        BorderColor = "#cccccc"
    };

    readonly C1Style _calendarHeaderStyle = new C1Style()
    {
    };

    readonly C1Style _calendarDayOfWeekStyle = new C1Style()
    {
    };

    static readonly C1Style CalendarDayStyle = new C1Style()
    {
        FontSize = C1StyleFontSize.Large

    };

    readonly C1Style _calendarToDayStyle = new C1Style(CalendarDayStyle)
    {
        BackgroundColor = "#efefef",
        Color = "#000000",
        FontWeight = "bold"

    };

    readonly C1Style _calendarSelectionStyle = new C1Style(CalendarDayStyle)
    {
        Color = "#000000",
        BackgroundColor = "#dedede",
        FontWeight = "bold"
    };

}



<style>


.status-list > li {
    display: inline; 
    margin: 7px;
}

    .c1-calendar {
        -webkit-box-shadow: 10px 10px 5px 0px rgba(0,0,0,0.23);
        -moz-box-shadow: 10px 10px 5px 0px rgba(0,0,0,0.23);
        box-shadow: 10px 10px 5px 0px rgba(0,0,0,0.23);
        margin: 20px;
    }
</style>

<_Sections>
    <Title>@CalendarRes.ItemTemplateTitle</Title>
    <Summary>@CalendarRes.OverviewSummary</Summary>
    <Description>@CalendarRes.ItemTemplateDescription</Description>
</_Sections>
